# API Base URL — used by the frontend to reach the backend
# Development: /api (Vite proxy handles forwarding to Django)
# Production:  set to full URL or /api if behind a reverse proxy
VITE_API_BASE_URL=/api
